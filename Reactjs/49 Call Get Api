Getapi.js {
  whenever we get we use Fetch().then() due to promise learned in Node js

  download npm install cors then require then use in node js 

  useEffect(()=>{
    fetch("http://localhost:4000/").then((resp)=>{
      resp.json().then((result)=>{
        console.log(result)
        setData(result);
        
      })
    })
  },[])

  
  try dynamic routing its important buddy
}